MONGODB_URI=mongodb://localhost:27017
MONGODB_DB_NAME=case_library
MONGODB_TIMEOUT_MS=5000
SQLITE_DB_PATH=./data/cases.db
AUTH_SECRET=change-me-in-local-env
AUTH_TOKEN_TTL=604800
CORS_ALLOW_ORIGINS=http://127.0.0.1:18080,http://localhost:18080

# OpenAI-compatible chat client settings for future AI review/skill integration.
# Fill these in local .env when AI-backed features are implemented or delegated.
AI_BASE_URL=https://api.openai.com/v1
AI_API_KEY=replace-with-local-secret
AI_MODELS=gpt-4.1-mini,gpt-4.1
AI_DEFAULT_MODEL=gpt-4.1-mini
AI_TIMEOUT_SECONDS=60
AI_REVIEW_ENABLED=false

# Frontend dev server API base. Keep /api for Docker Compose proxy/local same-origin use.
VITE_API_BASE_URL=/api

# Dev Container / Docker build mirrors. Override these in local .env if needed.
APT_MIRROR=https://mirrors.tuna.tsinghua.edu.cn/debian
APT_SECURITY_MIRROR=https://mirrors.tuna.tsinghua.edu.cn/debian-security
PIP_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple
NPM_CONFIG_REGISTRY=https://registry.npmmirror.com
FRONTEND_PORT=18080
